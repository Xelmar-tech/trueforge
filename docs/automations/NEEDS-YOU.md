# Needs you

Steps only a human can do. Newest first. Each entry says what to do and how the
build continues once it is done.

## 1. ~~Approve GitHub sudo mode for the App creation~~ — done 2026-09-05

You entered sudo mode and created the App `xelmar-foreman` (owner `Xelmar-tech`). The
agent then installed it on `Xelmar-tech/trueforge-automations-dogfood` (installation 159295128) from your signed-in Chrome session.

## Standing risk

- The Railway deployment has authentication off: anyone with the URL is an admin.
  Do not share the URL. Turning on OIDC is documented in `.railway/railway.ts`.
