---
'@truefoundry/trueforge-ui': minor
---

Add the Automations page (`/automations`, sidebar entry, `automations` route place), the
create/edit drawer with typed trigger conditions, lane parts and mode, the replay-in-shadow
test view, the Settings → Event sources tab with the GitHub App manifest flow, and an
`AutomationServer` port plus its harness adapter (`createAutomationServer`).
